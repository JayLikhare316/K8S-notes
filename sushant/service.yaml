apiVersion: v1
kind: Service 
metadata:
  name: nginx-service
  namespace: nginx-ns 
spec: 
  selector:
    app: nginx 
  ports:
    - protocol: TCP
      port: 80                   #---> This is the port we want to expose
      targetPort: 80             #---> This is the port where the container is listening
  type: ClusterIP